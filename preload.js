// Preload script for Electron
// This script runs before the renderer process and has access to Node.js APIs

window.addEventListener('DOMContentLoaded', () => {
  // You can add any preload functionality here if needed
});
